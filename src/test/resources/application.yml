session-rest:
  timeout-in-seconds: 1800
  persistent:
    namespace: test-application
    hosts: localhost
    password:
  in-memory:
    enabled: false

logging.level.com.github.zymen.springsessionrest: trace
